{% extends 'base.html.twig' %}

{% block title %}Rental index{% endblock %}

{% block body %}
    <h1>Rental index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>RentFrom</th>
                <th>RentTo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rental in rentals %}
            <tr>
                <td>{{ rental.id }}</td>
                <td>{{ rental.rentFrom ? rental.rentFrom|date('Y-m-d') : '' }}</td>
                <td>{{ rental.rentTo ? rental.rentTo|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('app_rental_show', {'id': rental.id}) }}">show</a>
                    <a href="{{ path('app_rental_edit', {'id': rental.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_rental_new') }}">Create new</a>
{% endblock %}
